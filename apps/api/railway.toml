[build]
builder = "NIXPACKS"
buildCommand = "cd ../.. && npm ci && npm run build --filter=@mathquest/api"

[deploy]
startCommand = "node apps/api/dist/main"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
